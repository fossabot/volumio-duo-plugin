{
	"name": "snapserver",
	"version": "1.0.0",
	"description": "SnapCast is a multiroom client-server audio player, where all clients are time synchronized with the server to play perfectly synced audio.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Saiyato",
	"license": "ISC",
	"volumio_info": {
		"prettyName": "snapserver",
		"icon": "fa-lightbulb-o",
		"plugin_type": "audio_interface"
	},
	"dependencies": {
		"fs-extra": "^0.28.0",
		"kew": "^0.7.0",
		"v-conf": "^1.4.0"
	}
}
